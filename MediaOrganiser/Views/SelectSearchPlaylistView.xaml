<Window x:Class="MediaOrganiser.Views.SelectSearchPlaylistView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MediaOrganiser.Views"
        mc:Ignorable="d"
        Title="Select Playlist" Height="160" Width="240">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1">

            <!-- Title -->
            <TextBlock Text="Select Playlist" Margin="0,0,0,10" FontSize="18"/>

            <!-- Submission -->
            <WrapPanel>
                <ListBox MinWidth="50" Margin="0, 0, 10, 0"/>
                <Button Content="Search Playlist" MinWidth="100"/>
            </WrapPanel>
        </StackPanel>

    </Grid>
</Window>
